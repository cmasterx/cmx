#pragma once

#include "flags.hpp"
#include <cstdint>

namespace cmx
{

using libversion_t = uint32_t;
static constexpr libversion_t LIBVERSION = static_cast<libversion_t>(20230723L);

} // namespace cmx
